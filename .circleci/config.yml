version: 2.1
orbs:
  ruby: circleci/ruby@0.1.2 

jobs:
  build:
    docker:
      - image: circleci/ruby:2.6.0
      environment:
        RAIL_ENV: test
    steps:
      - checkout
      - run: bundle install
